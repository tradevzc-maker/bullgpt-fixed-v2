const stages = ["Reading chart", "Detecting market structure", "Analyzing levels", "Evaluating setup", "Building scenarios", "Preparing BullGPT analysis"];
export function AnalysisLoading() { return <section className="loading-card" aria-live="polite"><div className="spinner" /><div><div className="section-kicker">BullGPT is analyzing</div><h2>Turning visual evidence into a decision brief.</h2><div className="stages">{stages.map((stage, index) => <span key={stage} style={{ animationDelay: `${index * 0.45}s` }}>{stage}</span>)}</div><p className="muted">This takes a moment. Analysis is based only on visible chart evidence.</p></div></section>; }
